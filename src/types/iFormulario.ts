export type FormularioType = {
  valor: number;
  tipo: string;
  descricao: string;
  };
  
  export type ExtratoItemType = {
    id: number; 
    data: string; 
    valor: number;
    tipo: string;
    descricao: string;
  };